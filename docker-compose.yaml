version: "3.9"

services:
  agent-marketplace:
    build:
      context: .
      dockerfile: Dockerfile
    image: aumos/agent-marketplace:latest
    container_name: agent-marketplace
    ports:
      - "8083:8080"
    environment:
      - PYTHONUNBUFFERED=1
      - LOG_LEVEL=INFO
    healthcheck:
      test: ["CMD", "python", "-c", "import urllib.request; urllib.request.urlopen('http://localhost:8080/health')"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 10s
    restart: unless-stopped
