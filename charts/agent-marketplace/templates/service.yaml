apiVersion: v1
kind: Service
metadata:
  name: {{ include "agent-marketplace.fullname" . }}
  labels:
    {{- include "agent-marketplace.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "agent-marketplace.selectorLabels" . | nindent 4 }}
