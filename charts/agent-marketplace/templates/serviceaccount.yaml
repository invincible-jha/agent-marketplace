{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ include "agent-marketplace.serviceAccountName" . }}
  labels:
    {{- include "agent-marketplace.labels" . | nindent 4 }}
automountServiceAccountToken: false
{{- end }}
